import TablesBoard from '@/components/register/TablesBoard'

export const metadata = { title: 'Tables' }

// /register — front of house (the counter iPad).
export default function TablesPage() {
    return <TablesBoard />
}
